pre-push:
  jobs:
    - name: format python
      run: uv run ruff format --check
    - name: lint python
      run: uv run ruff check
    - name: prettier
      run: prettier -w . --ignore-path .gitignore
