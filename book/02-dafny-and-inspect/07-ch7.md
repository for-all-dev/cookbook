# insert just the annotations without synthesizing the rest of it

it'd be great to show a red team that cheats really hard by editing the code in the loop. or a well-meaning sample that activated a drastic escape hatch of editing the source code.

Then we have to refactor a little to the _actual_ task, which is just insertions of annotations.

TODO: write the code that does this, version it, so both versions can exist in the repo at the same time.
TODO: write the chapter
